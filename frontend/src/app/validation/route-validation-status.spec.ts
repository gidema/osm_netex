import { RouteValidationStatus } from './route-validation-status';

describe('RouteValidationStatus', () => {
  it('should create an instance', () => {
    expect(new RouteValidationStatus()).toBeTruthy();
  });
});
